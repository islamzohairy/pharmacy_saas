/// Public API of the sales feature.
///
/// Other features import this barrel only — never this feature's internals
/// (no-cross-feature-internal-imports rule, GLOBAL_RULES.md).
library;

export 'domain/record_sale.dart';
